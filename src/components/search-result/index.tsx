import type { Component } from "solid-js";

export type SearchResultProps = Record<never, never>;

const SearchResult: Component<SearchResultProps> = () => {
  return <main class="container-lg">{}</main>;
};

export default SearchResult;
